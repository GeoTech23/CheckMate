import { styled } from 'styled-components';

const MessageContainerDiv = styled.div`
	display: flex;
	flex-flow: column nowrap;
	align-items: center;
	overflow-y: scroll;
	height: 500px;
	width: 600px;
`;

export default MessageContainerDiv;
